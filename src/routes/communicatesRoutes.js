/*
* Archivo con las rutas de comunicados - inquilino
*/

module.exports = (router) => {
    router.route('/communicates').get((req, res) => {
        throw new Error('Esto no esta implementado')
    });
}